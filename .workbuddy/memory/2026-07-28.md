# 2026-07-28 工作日志

## MySQL 连接修复 — Public Key Retrieval
- MySQL 8.0 用 `caching_sha2_password` 认证，JDBC 需允许公钥检索
- `application-dev.properties` 和 `application-prod.properties` 的 JDBC URL 添加 `allowPublicKeyRetrieval=true`

## 抽奖次数架构重构 — 按用户+活动独立存储

### 新增实体
- **`UserEventDrawCount.java`**: 关联 `user_id` + `event_id` + `draw_count`，唯一约束 `(user_id, event_id)`
- **`UserEventDrawCountRepository.java`**: `findByUserIdAndEventId` / `findByUserId`

### 后端改动
- **`UserService`**: 
  - `addDrawCount(userId, eventId)` → 创建或递增记录，返回 `UserEventDrawCount`
  - 新增 `getDrawCount(userId, eventId)` / `getUserDrawCounts(userId)`
  - 注入 `UserEventDrawCountRepository` + `RaffleEventRepository`
- **`UserController`**:
  - `POST /add-draw-count` → 改为接收 `{userId, eventId}` body
  - 新增 `GET /draw-count/{eventId}` → 公开接口，返回当前用户在某活动的次数
  - `userToMap()` → 移除 `drawCount` 字段（不再从 User 表读取）
- **`RaffleService`**:
  - `doRaffle()` → 从 `UserEventDrawCount` 表校验和扣减次数，不再用 `User.drawCount`
  - 注入 `UserEventDrawCountRepository`
- **`RaffleController`**:
  - `/draw` 返回的 `userDrawCount` 改为调用 `userService.getDrawCount(userId, eventId)`

### 前端改动
- **管理员 +1 次数**: 点击后弹出活动选择弹窗（加载所有活动），选活动 → 二次确认（方尼弹窗）→ 调用新API
- **管理员用户列表**: 移除旧的 `抽奖次数: N` 徽章（数据来源已失效）
- **用户抽奖页 `enterEvent()`**: 改为调用 `/api/user/draw-count/{eventId}` 获取该活动的次数
- CSS 新增 `.event-picker-*` 样式族

### 数据库
- `init.sql`: 新增 `user_event_draw_counts` 表，含 `(user_id, event_id)` 唯一索引
- `ddl-auto=update`: Spring Boot 启动时自动创建新表
- 旧 `users.draw_count` 列保留不动

## 抽奖次数预置初始化 (Fix undefined)
- **问题**: `user_event_draw_counts` 表无记录时，查询返回 null/undefined
- **RaffleService.createEvent()**: 创建活动后遍历所有已有用户，为每个用户插入 `draw_count=0` 的记录
- **UserService.login()**: 新用户注册后遍历所有已有活动，为每个活动插入 `draw_count=0` 的记录；添加 `@Transactional`
- 这样保证每个用户×活动组合永远有一条记录，起点为 0，不再出现 undefined

## 手动插入管理员账号
- 在 `caohuaraffledrawing` 数据库的 `users` 表插入一条管理员账号：
  - account_id: `caohua123123`
  - real_name: `管理员`
  - admin: `1`
  - draw_count: `0`

## 管理员用户管理 — 新增编辑/删除功能

### 后端
- **Repository**: `UserEventDrawCountRepository` 新增 `deleteByUserId`/`deleteByEventId`; `RaffleResultRepository` 新增 `deleteByUserId`/`deleteByEventId`
- **UserService**: 新增 `updateUser(userId, accountId, realName)` — 校验工号/姓名不与其他人冲突; 新增 `deleteUser(userId)` — 级联删除 raffle_results + user_event_draw_counts + user
- **UserController**: 新增 `PUT /api/user/update/{id}` 和 `DELETE /api/user/delete/{id}`（管理员权限）
- **RaffleService.deleteEvent()**: 扩充级联删除 — 先删 raffle_results 和 user_event_draw_counts，再删 lottery_event_prizes，最后删 event

### 前端
- **`loadAdminUsers()`**: 每个用户卡片新增 ✏️编辑 和 🗑️删除 按钮
- **`editUser(userId, ...)`**: 弹出方尼机器人对话框，可修改工号和姓名
- **`deleteUser(userId, ...)`**: 方尼机器人二次确认后调用删除API
- **`deleteEvent()`**: 改用 showConfirmDialog（方尼弹窗）替代原生 confirm()
- **`deleteResult()`**: 同上
- **CSS**: 新增 `.edit-user-fields` `.edit-user-row` `.edit-user-input` 样式

## 方尼弹窗 HTML 标签修复
- `showFangniAlert` 和 `showConfirmDialog` 中消息内容从 `escapeHtml(message)` 改为 `${message}`，使 `<br/>` `<b>` 正常渲染

## 管理员用户管理 — 新增"新建用户"和"Excel批量导入"

### 依赖
- `pom.xml` 新增 `org.apache.poi:poi-ooxml:5.3.0`

### 后端
- **`UserService`**:
  - `createUser(accountId, realName)` — 新建单个用户（校验唯一性，初始化抽奖次数）
  - `importUsersFromExcel(InputStream)` — 解析 .xlsx 文件，跳过首行表头，遇到工号/姓名重复或为空则记录异常，最后返回 `{total, successCount, skipped: [{row, accountId, realName, reason}]}`
  - `getCellStringValue()` — 辅助方法处理数字/公式等单元格类型
- **`UserController`**:
  - `POST /api/user/create` — 接收 `{accountId, realName}`（管理员权限）
  - `POST /api/user/import-excel` — 接收 `MultipartFile`（管理员权限）
- **`application-dev.properties`**: 添加 `spring.servlet.multipart.max-file-size=10MB`

### 前端
- **`index.html`**: 搜索框右侧添加 `+ 新建用户` 和 `📊 Excel导入` 按钮，隐藏的 `<input type="file">` 用于 Excel 上传
- **`app.js`**:
  - `showCreateUserDialog()` — 弹出方尼风格对话框输入工号和姓名，调用 `/api/user/create`
  - `handleExcelImport(fileInput)` — 读取文件，FormData 上传，解析结果后调用 `showImportResult`
  - `showImportResult(data)` — 弹出统计弹窗（总记录数/成功数/跳过数），异常记录以表格列出
- **`style.css`**:
  - `.search-box` 改为 flex 布局，`.search-input` 改为 `flex:1`
  - 新增 `.btn-create-user` `.btn-import-excel` `.import-result-*` 样式族

## 兑换码表 — redemption_codes
- 新增实体 `RedemptionCode.java` + Repository `RedemptionCodeRepository.java`
- 表结构: id, event_id→raffle_events(FK), code(unique), used(default false), used_by_user_id→users(nullable), used_at(nullable), created_at
- 启动后 JPA 自动建表（手动 MySQL 建表，ddl 在前面）

## 兑换码生成+导出功能
- **RedemptionCodeService**: `generateCodes(eventId, count)` 用 SecureRandom 生成13位数字字母混合码，防重复；`exportExcel(eventId, out)` 用 POI 导出含序号/兑换码/状态/使用人/时间的 Excel
- **RaffleController**: `POST /api/raffle/event/{id}/generate-codes` (body: {count})；`GET /api/raffle/event/{id}/export-codes` (直接下载 .xlsx)
- **RaffleService.deleteEvent()**: 级联删除 redemption_codes
- **前端**: loadEventInfo 每个活动卡片新增第二行按钮行：🎫生成兑换码（弹出数量输入框） + 📥导出兑换码（直接下载）
- **CSS**: `.btn-code-gen`(紫色) `.btn-code-export`(青色)

## 前端微调
- 搜索框恢复 `width:100%`（去掉 flex 依赖）
- "Excel导入" → "表格导入"
- 按钮移到搜索框下方（`.user-toolbar` 行）

## 兑换码弹窗 Bug 修复 (2026-07-28 12:00)
- **根因**: `.edit-user-overlay` / `.edit-user-box` CSS 类未定义，弹窗 DOM 被创建但完全不可见（无定位、无背景遮罩、无 flex 居中）
- **修复**: 在 `style.css` 末尾新增 `.edit-user-overlay` (position:fixed + flex 居中 + 半透明背景) + `.edit-user-box` (白色圆角卡片 + 动画) + 辅助样式（`.edit-user-header` / `.edit-user-fields` / `.edit-user-input` / `.edit-user-actions`），并新增 `@keyframes scaleIn` 动画
- **之前**: 点击按钮后弹窗 DOM 插入到 `<body>` 但 CSS 未定义，导致完全看不到任何变化，Console 无报错

## 兑换码 Bug 修复 (2026-07-28)
- **生成按钮无反应**: onclick 中 `escapeHtml(e.title)` 不转义单引号 `'`，标题含特殊字符时 JS 断裂。改为 `JSON.stringify(e.title)` 修复。
- **导出表格为空**: `<a>` 标签下载不带 session cookie，后端 isAdmin 检查失败返回 403 空内容。改为 `fetch` + `credentials: 'same-origin'` + blob 下载方式修复。

## 兑换码兑换抽奖次数 + 个人奖品模块 (2026-07-28 13:48)

### 后端
- **RedemptionCodeService.redeemCode()**: 校验兑换码是否存在→是否属于该活动→是否已使用→活动是否过期，通过后标记已使用(user+时间)并调用 UserService.addDrawCount 给用户该活动抽数+1
- **RaffleService.getUserPrizes()**: 按 userId 查所有中奖记录按时间倒序，过滤掉"感谢参与"
- **RaffleResultRepository**: 新增 `findByUserIdOrderByRaffleTimeDesc`
- **RaffleController**: 新增 `GET /api/raffle/my-prizes`（返回奖品名/图片/活动名/时间）；`POST /api/raffle/event/{id}/redeem-code`（接收 {code}，返回 success/message/drawCount）

### 前端
- **index.html**: 非管理员页面顶部新增「🎁 个人奖品」按钮；新增 `my-prizes-screen` 子页面（含返回按钮）；抽奖页面剩余次数下方新增「🎫 兑换码兑换抽奖次数」按钮
- **app.js**: `showMyPrizesScreen()` → 切换到个人奖品页加载数据；`loadMyPrizes()` → 调用 API 渲染奖品卡片列表（图片/奖品名/来源活动/抽中时间）；`showRedeemCodeDialog()` → 弹出方尼对话框输入兑换码，失败/成功均弹方尼提示，成功后刷新剩余次数
- **style.css**: 新增 `.my-prize-card` `.my-prize-img` `.my-prizes-list` `.redeem-code-overlay` `.redeem-code-box` `.redeem-code-input` 等样式族
