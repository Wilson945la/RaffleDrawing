# 2026-07-27 工作日志

## 草花互动抽奖服务器搭建

### 技术栈
- Spring Boot 3.3.2 + JPA + H2 内存数据库 + WebSocket
- 前端：纯 HTML/CSS/JS 单页应用（绿色主题 + 草花互动 Logo）
- Java 17 编译，JDK 26.0.1 运行

### 项目结构
```
RaffleDrawing/
├── pom.xml
├── src/main/java/com/caohua/raffle/
│   ├── RaffleApplication.java
│   ├── config/
│   │   ├── WebConfig.java (CORS)
│   │   ├── WebSocketConfig.java
│   │   └── DataInitializer.java (默认管理员: 13800138000/管理员)
│   ├── model/
│   │   ├── User.java (phone注册, admin标识)
│   │   ├── Prize.java (支持Base64图片)
│   │   ├── RaffleEvent.java (开始/结束时间)
│   │   └── RaffleResult.java
│   ├── repository/ (4个JPA Repository)
│   ├── service/
│   │   ├── UserService.java (登录即注册)
│   │   ├── PrizeService.java
│   │   └── RaffleService.java (权重随机抽奖)
│   ├── controller/
│   │   ├── UserController.java (register/login/logout/me)
│   │   ├── PrizeController.java (CRUD + 图片上传)
│   │   └── RaffleController.java (status/draw/results)
│   └── websocket/
│       └── RaffleWebSocketHandler.java (实时广播中奖)
└── src/main/resources/
    ├── application.properties (端口8080, H2内存库)
    └── static/
        ├── index.html (三页面: 认证/用户/管理)
        ├── css/style.css (绿色主题)
        ├── js/app.js (WebSocket + API交互)
        └── images/logo.png
```

### 核心功能
1. 手机号+真实姓名注册/登录（登录即注册）
2. 管理员(13800138000)可设置奖品（支持图片Base64、文字说明）、设置抽奖时间
3. 普通用户抽奖（权重随机，每人限抽一次）
4. WebSocket实时广播中奖信息
5. 绿色色调主题 + 草花互动Logo + 背景水印

### 验证结果
- 服务器在 8080 端口成功启动
- 管理员登录/事件创建/奖品管理均正常
- 用户注册/抽奖/防重复抽奖/中奖列表均正常
- 使用 H2 内存数据库（重启数据丢失，可后续切换到MySQL）

### 部署就绪改造 (11:40)
- pom.xml 新增 mysql-connector-j 驱动
- 拆分配置为三文件: application.properties(公共) + application-dev.properties(H2) + application-prod.properties(MySQL)
- DataInitializer 改用 @Value 读取可配置的管理员账号
- 新增 init.sql: MySQL 自动建表+初始管理员
- 新增 Dockerfile / docker-compose.yml: 一键部署（含 MySQL 容器）
- 新增 start.bat / start.sh: 开发/生产模式启动脚本
- 生产模式通过环境变量配置: DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASS, SERVER_PORT
- 重新编译打包验证 dev 模式正常启动

### IDEA 配置修复 (13:00)
- 原 RaffleDrawing.iml 是普通 Java 模块，非 Maven 模块，导致依赖无法解析
- 删除旧 .iml，新建 raffle-drawing.iml（isMavenModule=true）
- 更新 .idea/misc.xml 添加 MavenProjectsManager
- 更新 .idea/modules.xml 指向新模块文件
- 用户需在 IDEA 中右键 pom.xml → Reload Project

### 二维码生成 (13:20)
- 电脑局域网 IP: 172.16.162.87
- 用 Python qrcode 库生成绿色主题二维码（含草花互动 Logo）
- 二维码指向 http://172.16.162.87:8080
- 保存为 qrcode.png

### 跨网访问问题 (13:40)
- 手机 WiFi IP 与电脑前两位相同（172.16.x.x），实为同一局域网
- 访问失败原因：Windows 防火墙未放行 8080 端口
- 生成 open-firewall.bat 脚本，需右键管理员运行放行端口

### 编码 Bug 修复 (14:09)
- 管理员登录报「姓名与手机号不匹配」
- 根因: Spring Boot 默认 ISO 8859-1 读 .properties，中文 "管理员" 被破坏
- 修复: application-dev.properties 和 application-prod.properties 中的中文改为 Unicode 转义 \u7ba1\u7406\u5458
- 验证: 重新编译后管理员登录成功

### init.sql 与实体对齐 + 部署流程优化 (14:11)
- init.sql 中字段名与 Java 实体对齐（admin/display_order/image_base64/raffle_time）
- application-prod.properties 改为 ddl-auto=update（首次部署自动建表，安全不删数据）
- 部署流程: 本地 H2 开发 → 服务器 MySQL + prod profile → JPA 自动建表 + DataInitializer 自动创建管理员

### 中奖概率功能 (14:38)
- prizes 表新增 `probability DOUBLE DEFAULT 0.0` 字段，存小数（0.1=10%）
- 抽奖逻辑从"按库存权重"改为"按概率权重"：累加所有available奖品概率，生成随机数按区间命中
- 概率全为0时自动降级为等概率
- 前端：管理员创建/编辑奖品时输入百分比（0-100），内部/100存为小数
- 奖品列表展示概率百分比
- 已验证：MySQL建列、API返回、抽奖命中均正常

### 本机 MySQL 对接 (14:22)
- 本机已安装 MySQL 8.0，root 密码: caohuamoyu
- 创建数据库 RaffleDrawing (utf8mb4)，含4张表:
  - users: id, phone, real_name, admin, created_at
  - prizes: id, name, description, image_base64, quantity, remaining, display_order
  - raffle_events: id, title, start_time, end_time, active, created_at
  - raffle_results: id, user_id(FK→users), prize_id(FK→prizes), raffle_time
- application-dev.properties 改为 MySQL: localhost:3306/RaffleDrawing, ddl-auto=update
- 连接编码: characterEncoding=UTF-8（MySQL JDBC 不支持 utf8mb4 参数名，用 UTF-8 即可）
- 端到端测试通过: 管理员登录 → 创建活动 → 添加奖品 → 用户注册 → 抽奖 → 防重复 → 结果列表
- MySQL 数据验证: users=2, prizes=2, results=1, events=1, 全部持久化成功

### 管理员修改/删除中奖记录 (14:50)
- 后端新增:
  - PrizeService.increaseRemaining() — 归还奖品库存
  - RaffleService.updateResult(id, userId, prizeId, raffleTime) — 含用户验证、奖品库存同步、WebSocket广播
  - RaffleService.deleteResult(id) — 删除记录并归还库存
  - RaffleController: PUT /result/update/{id}, DELETE /result/delete/{id}
  - UserController: GET /list (管理员专用用户列表)
  - resultToMap 新增 userId/prizeId 字段供前端编辑用
- 前端新增:
  - 管理后台「中奖记录」Tab 每条记录对应编辑/删除按钮
  - 编辑弹窗：用户下拉(含姓名+手机号)、奖品下拉(含剩余库存)、中奖时间选择
  - WebSocket 监听 result_changed 事件自动刷新
- 编译通过，端到端测试: 修改用户、删除记录、库存归还均正常

### 登录注册逻辑改造 - 方尼提示语 (15:11)
- 登录逻辑改为区分三种情况（不再自动注册）:
  - 电话存在+姓名正确 → 正常登录
  - 电话存在+姓名不匹配 → NAME_MISMATCH: "抱歉，方尼觉得您的名字或电话填错了，请确认一下是否正确哦~"
  - 电话不存在 → NOT_REGISTERED: "方尼发现您还没有账号请先注册一下吧~" → 前端自动跳注册tab
- 注册逻辑:
  - 电话或名字任一已存在 → ALREADY_REGISTERED: "方尼注意到您已经有账号请去登录吧~" → 前端自动跳登录tab
  - 提交前二次确认弹窗: "方尼温馨提示请您确认自己的名字和电话是正确的否则会影响奖品兑换哦~"
- 前端新增:
  - 电话输入框纯数字过滤 + 11位格式校验 + 错误提示"请输入正确的电话号码"
  - 注册表单底部方尼说明注释: "电话和姓名仅用于工作人员发放奖励时的比对工作，不用于其他用途"
- 后端: UserController.codeError() 解析 "CODE:message" 格式 → 响应增加 code 字段
- UserRepository 新增 existsByPhone/existsByRealName
- 6项API测试全部通过

### 认证系统重构 — 账号ID模式 (16:03)
- **彻底取消注册/手机号**，改为单一登录页 + 账号ID机制:
  - User实体: `phone` → `account_id` (VARCHAR(30) NOT NULL UNIQUE), `real_name` 改为可选(NULL)
  - UserService: 删除register(), login改为accountId登录(不存在则自动创建), 新增generateAccountId()
  - UserController: 删除/register端点, 新增GET /api/user/generate-id, login接收accountId+realName(选填)
  - DataInitializer: admin使用 chfz-00000000
- 前端登录页重新设计:
  - 页面加载时自动从服务端获取唯一账号ID(chfz-XXXXXXXX)
  - 账号ID展示区 + 📋复制按钮
  - 方尼提示: "这是您的账号请好好复制保存，忘记了就要开新的帐号了~"
  - "已有账号，请输入！"输入框(老用户粘贴旧ID)
  - 用户名输入框(选填)
  - 登录时弹出确认: "方尼建议您确认复制好自己的账号以便中奖后奖品的兑换哦~"
- MySQL数据库迁移:
  - users表: 删除phone列, 新增account_id列, real_name改为NULLABLE
  - 旧数据迁移: phone → account_id (chfz-前缀), 去重删除重复用户
  - raffle_results外键重建
- 管理员账号: chfz-00000000 / 管理员
- 6项API测试全部通过(生成ID/新建/管理员登录/无姓名/回登/空ID校验)
- 16:18 方尼图标替换为机器人图片 (images/robot.png)
  - index.html: 登录页方尼提示改用 <img src="images/robot.png">
  - app.js: 确认弹窗图标改用 <img>, 按钮文字"确认注册"→"确认"
  - style.css: 新增 .fangni-robot 样式(18px/60px), .cfm-icon 改为图片模式
  - 编译通过, 图片访问 200 OK (761KB)
- 16:30 抽奖次数+用户管理+搜索功能+Tab重排
  - User实体新增 drawCount (默认0), MySQL同步添加列
  - UserRepository: findByAccountIdContainingOrRealNameContainingIgnoreCase 搜索
  - UserService: searchUsers(keyword) / addDrawCount(id)
  - UserController: /list?keyword= 搜索, /add-draw-count/{id} POST
  - RaffleResultRepository: 新增关联字段模糊搜索方法
  - RaffleController: /results?keyword= 搜索参数
  - 管理Tab重排: 用户管理 → 中奖记录 → 奖品管理 → 活动设置
  - 用户管理面板: 搜索框(账号ID/用户名) + 用户竖排列表 + +1次数按钮 + 二次确认弹窗
  - 中奖记录面板: 新增搜索框(用户名/奖品名)
  - 前端300ms防抖搜索, WebSocket变更自动刷新
  - API测试全部通过
- 17:05 中奖记录"已处理"功能
  - RaffleResult实体新增 processed 字段 (BOOLEAN, 默认false)
  - MySQL raffle_results表新增 processed 列
  - RaffleService: processResult(id) 标记已处理, updateResult/deleteResult 对已处理记录返回错误
  - RaffleController: PUT /result/process/{id} 接口, resultToMap 追加 accountId/processed 字段
  - 前端中奖记录展示: 账号ID (chfz-xxx格式) + 用户名 → 奖品名, 含中奖时间
  - 每条记录右侧: 大确认按钮「确 认」+ 小编辑/删除图标
  - 点击确认 → 方尼弹窗 "方尼提醒您：是否确认该条奖励记录已兑换？" → 确认后标记已处理
  - 已处理记录: 背景变浅绿色, 显示 "✓ 已处理" 徽章, 隐藏确认/编辑/删除按钮
  - init.sql 同步更新, ddl-auto=update 自动加列
  - 编译部署成功, API返回accountId/processed正常
  - 调整确认按钮布局: 确认按钮改为竖向排列在修改/删除按钮下方 (.result-item-actions flex-direction: column)

## 架构重��：奖品管理与活动设置分离

### 核心改���
- **Prize实体**: 移除 quantity/remaining/probability 字段，奖品变为纯模板（名称/描述/图片）
- **新建 LotteryEventPrize 实体**: 关联活动与奖品，含 quantity/remaining/probability
- **新建 LotteryEventPrizeRepository**: CRUD + 按事件ID查询
- **PrizeService**: 简化为纯CRUD，移除库存和概率相关方法
- **PrizeController**: 移除 quantity/probability 参数
- **RaffleResult**: 新增 eventId 字段关联 RaffleEvent
- **RaffleService**: 
  - createEvent 接受 prizeConfigs 列表（prizeId/quantity/probability）
  - doRaffle 改为基于 lottery_event_prizes 的加权随机
  - updateResult/deleteResult 的库存归还改为通过 event_prizes
  - 新增 activateEvent/deactivateAllEvents
- **RaffleController**: 
  - 新接口: GET /events, GET /event/{id}/prizes, POST /event/create, PUT /event/update/{id}, DELETE /event/delete/{id}, PUT /event/activate/{id}
  - 旧的单一活动创建接口被替换
- **RaffleEventRepository**: 新增 findFirstByActiveTrue, findByActiveTrue (list)

### 前端改动
- **奖品管理**: 添加/编辑奖品不再有数量/概率字段
- **活动设置**: 完全重写
  - 活动列表卡片展示（标题/时间/状态/操作按钮）
  - 新建/编辑活动弹窗：选择奖品、设置数量和概率
  - 支持启用/编辑/删除活动
  - 同一时间只有一个活动激活

### 数据库 (init.sql)
- prizes 表移除 quantity/remaining/probability
- 新建 lottery_event_prizes 表 (event_id, prize_id, quantity, remaining, probability)
- raffle_results 新增 event_id FK

### 编译验证
- 编译成功 (21源文件)
- API测试通过: status/events/create/get prizes/draw

## Bug修复: 编辑活动时标题无法修改
- 原因: index.html 中活动标题展示 div (`id="event-title"`) 与 modal 输入框 (`id="event-title"`) id 重复
- JS getElementById('event-title') 总是返回 DOM 中第一个元素（div），而非 modal 中的 input
- 修复: 展示 div id 改为 `event-title-display`，JS 中对应引用同步更新

## Bug修复: 中奖记录搜索缺失 accountId
- 原因: RaffleResultRepository 用 JPA 方法衍生查询只搜索 `user.realName` 和 `prize.name`，未包含 `user.accountId`
- 中奖记录展示第一行就是账号ID，但搜索账号ID返回空
- 修复: 用 @Query JPQL 改写 `searchByKeyword`，三字段模糊匹配 (accountId/realName/prizeName)

## 非管理员页面重构：活动选择 + 抽奖次数 + 奖品展示

### 新API
- `GET /api/raffle/running-events` — 公开接口，返回所有进行中的活动列表
- `GET /api/raffle/event/{id}/prizes-public` — 公开接口，返回活动奖品（名称/图片/说明/剩余）
- `POST /api/raffle/draw` — 改为接收 `eventId`，检查 `drawCount > 0`，中奖后自动递减
- `GET /api/raffle/my-result?eventId=` — 改为接受 eventId 参数

### 后端改动
- `RaffleService.doRaffle(user, eventId)` — 按活动ID抽奖，校验 drawCount，中奖后 drawCount-1
- `RaffleController.draw` — 接受 JSON body `{eventId}`, 返回结果含 `userDrawCount`

### 前端改动
- 用户登录后显示「进行中的抽奖活动」列表（卡片式）
- 点击活动卡片 → 进入该活动的抽奖页面
- 显示「剩余抽奖次数」徽章（从 DB drawCount 读取）
- 抽奖按钮：drawCount=0 显示「无次数」灰掉；已中奖显示「已抽奖」
- 活动奖品网格：2列卡片展示（奖品图片+名称+剩余数量）
- 返回按钮回到活动列表
- WebSocket 通知更新更智能：event_update 刷新活动列表，result_changed 刷新中奖名单

## 管理员页面改进：中奖记录活动名 + 多活动共存

### 中奖记录显示活动名 (17:50)
- 后端 `resultToMap()` 早已返回 `eventTitle` 字段，前端 `loadAdminResults()` 之前未渲染
- 修复: 在账号ID行旁边新增 `<span class="result-event-tag">` 显示活动名
- CSS 新增 `.result-event-tag` 样式（主题色背景、小圆角标签）

### 活动设置改为多活动同时开启 (17:50)
- **需求变更**: 去掉互斥逻辑，允许多个抽奖活动同时处于启用状态
- **后端 RuankeService**:
  - `activateEvent()` — 移除 `deactivateAllEvents()` 调用，直接 `setActive(true)`
  - `createEvent()` — 移除 `deactivateAllEvents()` 调用，新建活动不再影响已有活动
  - 新增 `deactivateEvent(eventId)` — 停用单个活动
  - 删除 `deactivateAllEvents()` 私有方法
- **RaffleController**:
  - 新增 `PUT /event/deactivate/{id}` 接口（管理员权限）
- **前端 app.js**:
  - `loadEventInfo()`: 活动卡片根据 `e.active` 显示「停用」或「启用」按钮（之前只在 `!e.active` 时显示启用按钮）
  - 新增 `deactivateEvent(id)` 函数 — 调用 `/deactivate/{id}`，确认文案：「确定要停用这个活动吗？已抽奖记录不受影响。」
  - `activateEvent()` 确认文案简化为「确定要启用这个活动吗？」（去掉「其他活动将被停用」）
- **CSS**: 新增 `.btn-deactivate` 橙色停用按钮样式
- 编译通过 BUILD SUCCESS
