# ============================================================
# 开发环境 (dev) — 本机 MySQL
# 激活方式: spring.profiles.active=dev （默认）
# ============================================================
server.port=8080

# MySQL 数据库 — 本机
spring.datasource.url=jdbc:mysql://localhost:3306/RaffleDrawing?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=caohuamoyu

# JPA — update 模式：新增表/列，不删已有数据
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# 默认管理员
app.admin.account-id=chfz-00000000
app.admin.name=\u7ba1\u7406\u5458
